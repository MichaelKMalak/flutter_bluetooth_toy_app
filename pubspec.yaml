name: bluetooth_toy_app
description: Bluetooth Toy App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_blue: ^0.8.0
  fluttertoast: ^8.0.3
  google_fonts: ^2.0.0

dev_dependencies:
  lint: ^1.5.3

flutter:
  uses-material-design: true
